# ==============================================================================
# UFIS Frontend - Production Environment (Vercel)
# ==============================================================================
# CURRENT SETUP: Direct EC2 connection (most reliable for HTTP EC2 backends).
# The backend CORS is configured to accept all *.vercel.app origins.
#
# NOTE: Browser may block HTTP requests from HTTPS pages (Mixed Content).
# If that happens, switch BACK to proxy mode:
#   VITE_API_BASE=/api/v1   (and ensure vercel.json proxy is in place)
#
# STRATEGY 1 (Current - Direct EC2, CORS-based):
# ------------------------------------------------------------------------------
VITE_API_BASE=http://13.60.249.74:8000/api/v1
VITE_API_BASE_URL=http://13.60.249.74:8000/api/v1

# STRATEGY 2 (Proxy via vercel.json - switch if Mixed Content blocks occur):
# ------------------------------------------------------------------------------
# VITE_API_BASE=/api/v1
# VITE_API_BASE_URL=/api/v1
